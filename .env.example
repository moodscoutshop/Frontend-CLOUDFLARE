# Backend API base URL
REACT_APP_API_URL=http://localhost:5000

# Debug mode: 'true' shows verbose console logs in the browser, anything else
# suppresses them so production users never see internal debug output.
REACT_APP_DEBUG_MODE=false
